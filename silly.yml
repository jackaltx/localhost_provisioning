---

- name: Podman Installation
  hosts: localhost
  connection: local

  tasks:

    - ansible.builtin.debug:
        var: telegraf_influx_url

    - ansible.builtin.debug:
        var: telegraf_influx_token
